import { FC } from "react";
import { Text, View } from "react-native";

const Dashboard: FC = () => {
    return (
        <View>
            <Text>Dashboard</Text>
        </View>
    )
}

export default Dashboard;